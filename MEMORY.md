# MEMORY.md — agent working memory

> Read this first in every new session. Living facts only — history lives in git.

## Status

- **v0.6.45 (2026-09-02).** (1) **Question forms revived — one form per question.** Clickable options + "Other…" free text (`Feed.tsx` QuestionCard adapted from the pre-0.6.24 card; `QuestionAsText` removed; streaming JSON-string input parsed via new `webview-src/lib/questions.ts`). **Design decision (P0-grade):** no published OpenCode build ships question-reply routes (404 on beta-18314) → when a question tool parks, App fires `prompt.interrupt` ONCE per tool call (`handedBackIds` guard, same path as ■ Stop); UI un-busies via derived `busy = busySessions[activeId] && !hasOpenQuestion`; every answer is sent as a REAL chat message — plain when idle (starts a new turn), `sendMessage` falls back to `delivery:"steer"` when `busySessionsRef` says the session still runs, explicit queue toggle wins. **Answered-ness must live in App state** (`questionAnswers[sessionId][toolCallId][qi]`) — server refetches replace tool parts wholesale, so part-state markers never survive. (2) **0.6.44 correction:** `affectsConfiguration("opencode2.server.listen")` never matches (VS Code segment-boundary rule) → both branches were dead; now an explicit six-key check in `extension.ts`. (3) **Security:** companion server validates `Host` (`parseHostHeader` in `listenConfig.ts`; missing→400, loopback-bind + non-loopback Host→403 — a rebound request carries no Origin, so the 0.6.44 origin gate missed it). (4) Single-notify listen errors (permanent error handler only post-listen); atomic `companion.update` (validate-all-then-write); UsageDrawer `next!` removed + stale-response guard (`loadSeqRef`); CompanionDrawer copyURL toast. Gates green: typecheck · 156 tests · build · audit CLEAN.
- **v0.6.44 RELEASED (2026-09-01).** (1) **Start/stop race fixed** — `ExtensionServer.start/stop` serialize through a `lifecycle` promise chain (`src/extensionServer.ts`); previously drawer `companion.update` called `start()` directly (`rpc.ts`) while the same config writes fired `syncExtensionServer()` → double server creation, spurious EADDRINUSE warnings, potential leaked listener. (2) **Origin gate (security)** — non-preflight requests carrying a non-allow-listed `Origin` get `403` before proxying; browser "simple requests" skip preflight and CORS only blocks *reading*, so daemon auth injected server-side made a loopback no-password server drive-by executable. Native apps send no Origin → unaffected. (3) `handleRequest` wrapped in try/catch → `500` (was unhandled rejection + hanging socket on e.g. malformed discovery URL). (4) Client aborts (`res.close` before `writableEnded`, `req.error`) destroy the upstream request — aborted SSE clients no longer leak daemon sockets. (5) `POST /opencode2/extension/start` returns the reachable companion URL as `url` + daemon endpoint as informational `daemon` (was the daemon loopback URL the remote caller couldn't reach). (6) `server.listen*` config changes no longer trigger a full daemon `connect()` (`extension.ts` narrowed the trigger). Pure helpers (`ListenConfig`/`isLoopback`/`expectedAuthHeader`) moved to vscode-free `src/listenConfig.ts` so node tests can import them. Known-remaining (P3): no `Access-Control-Max-Age` on preflight, `0.0.0.0` URL copied verbatim, no remote health endpoint, password round-trips to webview (form prefill). Gates green: typecheck · 149 tests · build · audit CLEAN.
- **v0.6.38 RELEASED (2026-08-31).** (1) **Silent hidden start (Windows)** — `src/cli.ts` now prefers sibling `.exe` / direct `node <js>` over `cmd.exe /c` shim (parses `.cmd` shim `"%~dp0\..."`), fallback `cmd.exe /d /s /c` quoted; `src/controller.ts` `startHiddenService` normalizes to `/d /s /c` + `shell:false` + `windowsHide:true`; `src/locations.ts` expanded win32 probes (`%APPDATA%\npm\<name>.exe`, `.cmd`, `node_modules/@opencode-ai/cli/bin` + `opencode-ai/bin` exe+extensionless); `scripts/verify-hidden-start.mjs` exercises both exe/shim paths. (2) **Model Manager fixes** — `ModelManager.tsx:62` `all/hidden/visible` select now themed `.filter-select` (`styles.css:630`) matching `--oc2-*` tokens; provider `hide all/show all` now uses full provider list (`ModelManager.tsx:102` `fullProviderModels`) so filter does not skew label/toggle; eye `👁/🚫` per-model hide/show confirmed with drawer ↔ picker round-trip (`lib/models.ts:62`, `Composer.tsx:410`). Gates green: typecheck · 140 tests · build · audit CLEAN.
- **v0.6.37 RELEASED (2026-08-29).** Inbox polish (TUI `queued-menu` parity): (1) **Edit queued** — `InboxDrawer.tsx:60` adds `✎ edit` → `inbox.cancel` + `onEdit` → `composerPrefill` (`App.tsx:2240`), ghost click still opens inbox. (2) **Files carried** — `payload.files[]` now parsed in `InboxDrawer.tsx:36` + `App.tsx:706` + `Feed.tsx:66` (`📎` chip in drawer meta + ghost text `truncate(files)`). (3) **Fallback poll** — drawer `3s` interval (`InboxDrawer.tsx:51`) + `App.tsx:1218` busy poll for `refreshQueued` alongside `refreshMessages` (was event-only, stalled on SSE drop). (4) **Delivery hint + badge** — header micro `steer · queue` + `chip on` when `steer` (`InboxDrawer.tsx:68`). (5) **Polish** — `busyId` disables chips during act, `inbox-actions` class replaces inline `rowdel` hack (`styles.css:1494`). Gates green: typecheck · 140 tests · build · audit CLEAN.
- **v0.6.36 RELEASED (2026-08-28).** Subagent polish: (1) **Tail pinned newest-at-bottom** — `SubagentsDrawer.tsx:40` adds `tailRef` + effect `scrollTop = scrollHeight` on every tail update (like main Feed), chronological `slice(-40)` now stays peer usual flow; fixes wrong-direction scroll. (2) **Bottom strip grouped + ephemeral (TUI-like)** — `App.tsx:177` groups `childSubs` by `agent` (`groupedSubs` Map), shows `🤖 general ×3 ●` instead of N pills (cap 3 groups + `+N more`), `hasActiveSubagents` via `isSubagentActive`; strip `hidden when 0`, `⚡ active/total` pulsing when active, `0/N` grace 4s then auto-hide (matches TUI `PR #23785`/`subagent-footer.tsx` ephemeral dock, not sticky). (3) **Dot class fix** — `SubagentsDrawer.tsx:125` `dot ok busy` → `dot busy` (existing `dot.busy` pulse in `styles.css:1890`). Verified: upstream `subagen` toast is server `tui.toast.show`, not this extension (`notifications.ts:28` only `agent finished`, `\bsubagen\b=0`). Gates green: typecheck · 140 tests · build · audit CLEAN.
- **v0.6.35 RELEASED (2026-08-28).** Fix start/busy/branch trilogy: (1) **Auto-start now opt-in** — `opencode2.server.autoStart` default `false` (`package.json:224`, `controller.ts:399`); `connect`/`restart`/`cli.start` take `force:true` so *Restart Background Service* / Start CTA always spawns even when autoStart off; `scheduleRetry` respects `force`; extend shim resolution (`src/locations.ts:24` adds `%APPDATA%\npm\{name}.cmd`), `startHiddenService` pipes stderr + `windowsHide` + exit-code check, `waitForDiscovery` 15→30s, `connectTimeout` 20→35s, drop redundant `isHealthy` double-probe (trust `Service.discover`). (2) **Busy after reload** — `webview-src/App.tsx:314` `refreshActiveSessions` hydrates `busySessions` from `sessions.active` on (re)connect/resync, retry pill hydrates even when `!busy`. (3) **Branch chip** — `HeaderBar` chip now scoped to `composerDirectory` (`App.tsx:722,738,1550`), diff via read-only `opencode-working-diff:` provider (`src/workingDiffDocs.ts`) instead of untitled `diff` doc (no save prompt). Gates green: typecheck · 140 tests · build · audit CLEAN.
- **v0.6.34 RELEASED (2026-08-28).** Usage drawer now scoped: **Total · Project · Session** (`webview-src/components/UsageDrawer.tsx`) — chip row reuses slash `oc2-pop-fchip` style (`Composer.tsx:642`). Total = global `session.stats`; Project = `project.current`→`session.stats {project}` with client-aggregated `session.list` fallback filtered by `workspaceDir`; Session = single active session synthesized from `SessionSummary`. `src/apiAdapter.ts:279` now accepts `{project}` + `rpc.ts:88` forwards it. `by model` now filtered to actually-used tokens only (`total tokens >0` after `aggregateModels`). `webview-src/styles.css:1774` `.usage-scope`/`.usage-hint`.
- **v0.6.33 RELEASED (2026-08-28).** Scoped usage drawer initial (Total/Project/Session) — same as 0.6.34 but before model-filter fix (overwritten on disk; git history has 0.6.34 with filter).
- **v0.6.32 RELEASED (2026-08-28).** Fix plan-accent flicker: user bubbles tagged `planAtSend: "plan"|"build"` now survive server refetches via session-scoped `planOverlayRef` in `App.tsx:129` (`plan`/`build` explicit `if`s only, `custom/other` stays plain) plus `Feed.tsx:96` hybrid explicit+reverse-walk inference (no `else` coercion). Pending→real id migration + 1k cap. `agentKind` now threads `App:228` → `Feed:agentKind` instead of dead `isPlan` boolean.
- **v0.6.31 RELEASED (2026-08-28).** package.json bumped 0.6.28 → 0.6.31 (matches the three milestone labels below — one version per phase, shipped together); vsix packaged via `npm run package` (gitignored per convention). Ships: session awareness (unread dots, cross-window running pulse), opt-in auto-title, Usage & stats drawer, VCS working-tree badge + the vcs location-drift fix.
- **v0.6.31 (2026-08-28) — Phase C: VCS working-tree chip + vcs directory-drift fix.** (1) Branch chip is now a button → `onOpenWorkingDiff`, with a `.branch-diff` `+N −M` badge (sums of `vcs.status` `additions`/`deletions`; hidden when 0). (2) New `vcs.status` RPC → adapter `vcsStatus()` (`VcsStatusSummary`); refresh = connect + window-focus + the `vcs` event-batch slot (`filesystem.changed` → "vcs" → 150ms batch → **trailing 1200ms timer** via `scheduleVcsCounts` — never a git scan per event). (3) **SDK drift, P0-grade:** the pinned beta SDK types vcs inputs double-nested (`{location:{location:{directory}}}`) but the live server reads a flat `location[directory]=` query — SDK-typed calls were **silently ignored** and resolved to the home dir, so `vcs.get()` never returned a branch (the existing branch chip was effectively dead) and status would always be empty. Module-level `vcsLocation()` + `Parameters<>` casts bridge the drift; the webview sends `directory: workspaceDir` (canonicalized in rpc.ts, same pattern as @-mentions). Live-verified via `scripts/vcs-probe.mjs`: status lists dirty files incl. untracked with line counts (`+208 −12` on a dirtied tree), get returns `{current:"master",default:"master"}`. Gates green: typecheck · 140 tests · build · audit CLEAN.
- **v0.6.30 (2026-08-28) — Phase B: Usage & stats drawer.** RPC `session.stats` → adapter `sessionStats()` (normalized `SessionStats` in protocol.ts; `finiteNum` coercion everywhere so NaN never reaches the UI). Live-verified via `scripts/stats-probe.mjs`: the bare call returns `tools.mode: "summary"` (the SDK's tools-detail input is ignored by the server — detail rows render only if the server default flips; adapter handles both), **`models[].model` is a flat `"provider/id"` STRING, not a `{providerID,id}` object** (SDK type lies — adapter accepts both shapes), and stats are global across projects so model rows arrive duplicated per-directory (drawer aggregates + sorts by cost). UI: `UsageDrawer.tsx` (grid: sessions/prompts/steps/cost/streak/active-days · pure-CSS activity bars, last 42 days · tokens · tool reliability · by-model spend) opened from the **StatusStrip token/cost cluster** (now a `.stat-cluster` button; counters all kept — frozen-anchor rule: additive only) and `⋯ → Usage…` (session-independent, after Inbox…). CSS uses only existing `--oc2-*` tokens. Audit-accuracy fix: `audit-consistency.mjs` component list was missing SubagentsDrawer/PlansDrawer → false CSS orphans; now scans all 16 components + `lib/markdown.ts` (template-string classes `.file-link/.diff-line/.md-diff` were false positives too) — audit now CLEAN, 0 findings. Gates green: typecheck · 140 tests · build · audit CLEAN.
- **v0.6.29 (2026-08-28) — Phase A: ambient awareness & auto-title.** (1) **Unread dots** — server-authoritative `time.idle`/`time.viewed` on `SessionSummary` (`webview-src/lib/rpc.ts`); `SessionsDrawer` shows an accent dot + bolder title when `idle > viewed`, and opening a session fires `session.view({sessionID, idle})` (App effect on `activeId`/`sessions`, with optimistic local `viewed` bump so refresh cycles don't re-fire). Dot clears cross-window/TUI for free (shared service). (2) **Cross-window running pulse** — `sessions.active` RPC → adapter `sessionActive()` (normalizes bare object-map `{id: {type:"running"}}`, array, and `{data}` wrappers) → `activeRunning: ReadonlySet` in App, refreshed on connect, resync, and every `sessions` refresh tick; pulsing `session-dot.running` on drawer rows. (3) **Auto-title** — new host-side `AutoTitleWatcher` (`src/autoTitle.ts`, mirrors AutoCompactWatcher): on `session.execution.succeeded`, opt-in `opencode2.sessions.autoTitle` (default false — spends provider tokens), `session.generate` (transient, non-mutating) then `session.rename` ONLY if still default title (race guard re-fetches; never clobbers a manual title). RPC +`sessions.active`/`session.view` (protocol.ts + rpc.ts, `num()` helper added); adapter +`sessionActive/sessionView/sessionGenerate`. Gates green: typecheck · 140 tests · build · audit (91 union methods).
- **0.6.x bug-fix pass (2026-08-28)** — robustness hardening: (1) `AutoCompactWatcher.refreshLimits` now goes through `createApi().models()` (adapter normalizes bare-array vs `{data}` list shapes) instead of reading `res.data` directly — a shape drift previously silently killed context-limit caching and disabled auto-compaction; (2) `totalTokens()` guards a missing `cache` sub-object (was `cache.read` TypeError; `contextPercent` already guarded) and `formatTokens()` now rejects non-finite input (`InfinityM` artifact); (3) smart-repair model selection `App.tsx:1177` excludes the just-failed model from the candidate set so it can't "switch" to the same broken default; (4) dead `.q-*` CSS (removed `QuestionCard`, v0.6.24) purged — audit orphans now only `.file-link`/`.diff-line`/`.md-diff` (TS-constructed, false positives). Noted: `opencode2.notifications.errors` was ALREADY in the schema (package.json:201, default false) — descriptor existing; NOT dead code (despite reading truncated long ago).

- **v0.6.28 (2026-08-27)** — Fix 0.6.27 regression: user bubbles no longer hidden — historic/untagged/other messages stay plain (no `data-plan`, `1px var(--oc2-border)` at `styles.css:783`), only **new** tagged messages get accent (`planAtSend: "plan"|"build"` tri-state via `agentKind` at `App.tsx:210,1098`, opt-in map in `Feed.tsx:98`). Build vs plan distinguished (`data-plan="build"`→`--oc2-user-build`, `"plan"`→`--oc2-user-plan` at `styles.css:45,792`; compat aliases for `true/false`), extra agents future-proof plain. Send arrow now SVG `14px` `strokeWidth=3` (`Composer.tsx:797`, `styles.css:1500` thicker than 18px text `↑`).
- **v0.6.27 (2026-08-27)** — `@` file mentions now scoped to the working folder (`Composer` sends `directory: active.location.directory ?? workspace.directory`, canonicalized; `src/rpc.ts:168` + `App.tsx:209` + `Composer.tsx:169,248` relative insertion), dead clickable text fixed (external `https` links now `url.open` instead of silent no-op, only file-like `code` gets pointer via `lib/markdown.ts:77` `file-link` + `Feed.tsx:569` `isFileLikeCode`, `styles.css:1132`), send arrow enlarged to 18px/800 weight, and **user bubbles now have a sticky subtle accent border** (`styles.css:792` `border-color: color-mix …` + 2px right edge via `--oc2-user-build/plan` per theme at `styles.css:45,91,139,175,211,247`; `Feed.tsx:86` reverse-walk from next assistant `agent` + optimistic `planAtSend` at `App.tsx:1092` keeps colour after agent switches). Also: reasoning/thinking text italic+muted persists.
- **v0.6.25 (2026-08-27)** — shell permissions no longer block: auto-acknowledged perms (autoAllow or session-auto-accept) render as non-blocking plain text (`.perm-text`), not the interactive Allow/Deny `PermissionRow`; only genuinely interactive (askFirst, not auto-accepted) perms keep the card so Deny stays possible. Resync re-sends lost replies (`lostReplyIds` clears the stale `RespondedTracker` mark for still-pending requests). Also: reasoning/thinking text now renders italic + muted (`--oc2-fg-muted`) to distinguish from the response.
- **v0.6.24 (2026-08-27)** — question tool now renders ALWAYS as inline plain text (no blocking interactive card). Removed `QuestionCard`/`OtherRow` and the whole `onAnswer`/`onQuestionReply`/`questionsDisabled` plumbing (Feed→MessageGroup→Part→ToolCard + App handlers). `QuestionAsText` shows options lettered **a)/b)/c)…** inline, so the user answers naturally in the chat. The interactive "question window" (old `QuestionCard`) got stuck — it stayed `running`/`streaming` and never resolved, breaking the conversation flow. `detectQuestionSupport()` + `ui.questionsSupported` still exist in controller/config but are now unused by the webview (no published build ships the reply routes anyway).
- **v0.6.23 (2026-08-27)** — question tool now auto-degrades **code-side** (no manual setting): `controller.detectQuestionSupport()` probes `/openapi.json` on connect and sets `ui.questionsSupported` in `ResolvedConfig`; the webview renders `QuestionAsText` (plain markdown, no card/reply attempt) whenever `!questionsSupported`, flipping to the interactive `QuestionCard` automatically once a stable build ships the routes. Removed the temporary `opencode2.ui.questionsDisabled` setting. Also: README WIP/experimental banner + `beta`/`experimental` marketplace tags (company confirmed **Anomaly** / `anomalyco` — the README's existing repo link was already correct). Previous: **v0.6.22 (2026-08-27)** — question tool graceful degradation: on server `QuestionHTTPUnavailable` / "no pending question", `onQuestionReply` re-marks the card `status:"unsupported"` + shows a clear notice (server build doesn't support question replies) instead of sending a misleading steered fallback that the agent can't see (the silent "sits and waits" hang). `QuestionCard` renders the unsupported state. No published OpenCode build registers `/api/question/*` (verified 404 on beta-18314 AND dev-18341; 0 mentions in openapi.json) — routes exist only in the unpublished dev branch, so a server-side fix is NOT distributable; client degrades for all users. Previous: **v0.6.21 (2026-08-26)** — hardened providerish regex (PROVIDERISH_RE in lib/failure.ts, unit-tested: Console Go invalid_request_error/upstream request failed/invalid parameters matched) + auto-recover once per session right after compaction: on providerish failure within 30s of compaction, auto model-switch+resend instead of requiring Retry click. Previous: **v0.6.20 (2026-08-26)** — clean release bump (no code changes; question-reply upstream gap + extern-dir UX shipped in 0.6.19). Previous: **v0.6.19 (2026-08-26)** — external_directory (out-of-project) now always prompts even in autoAllow, with a per-session "Allow for session" button; permission cards render even with no active session; notification Review selects the asking session. (Also: message actions, compact completed tool cards, question watchdog.) — message hover actions (copy/regenerate/edit-prefill), completed tool cards auto-compact, question watchdog clears busy+notice if server can't resume; permission cards render even with no active session and notification Review selects the asking session. Previous: **v0.6.18 (2026-08-26)** — question HTTP surface verified ABSENT on live server (all question* routes 404): reply 404s now throw code QuestionHTTPUnavailable; App forwards collected answers as one steered prompt WITHOUT alarming notice; re-pinned client @opencode-ai/client@beta-18314 (drift: +session.step.streamed, +session.message.content.updated routes). Previous: **v0.6.17 (2026-08-26)** — question stall fix: questionList merges Location-scoped /api/question/request + session list; App matches callID→messageID→id→first pending, pads answers, replies then surfaces notice + forwards only if truly no request — no more ghost/queued bubble; reminders stripped from assistant content too (ingestion + Feed render). Previous: **v0.6.16 (2026-08-26)** — question batch now wired to dedicated session.question.reply (question.list/reply RPCs + adapter wrappers + Feed onQuestionReply prop through ToolCard/QuestionCard); finish_reason/stream-ended provider errors treated as providerish so smart-retry switches model + retries after compaction. Previous: **v0.6.15 (2026-08-26)** — question batch via dedicated `session.question.reply` (`answers: string[][]`) with `Continue (X/Y)` for incomplete batches; `question.list`/`reply` wrappers in `apiAdapter` + `question.list`/`reply` RPCs, `Feed` accordion (`expandedIdx` + `answers[]`) and `App` ingestion filter before `setMessages` cover all agents.
- **v0.6.14 (2026-08-26)** — question batch via `onBatchReply` + `onQuestionReply` plumbing: all `qs` visible, one `expandedIdx` at a time, `Continue (X/Y)` fills remaining as `Unanswered` and batch-replies via `User has answered…` (steer), so incomplete batches no longer stall; ingestion filter at `App.tsx` before `setMessages` covers all agent reminders.
- **v0.6.13 (2026-08-26)** — question accordion: all questions visible, one expanded at a time in order (first expanded, others collapsed headers); answering collapses to `✓` and auto-expands next. Batch continuation: `Continue (X/Y)` fills remaining as `Unanswered` and submits via `User has answered…`, so incomplete batches no longer stall. Ingestion filter now strips system-reminders before `setMessages` (all agents, all shapes).
- **v0.6.12 (2026-08-26)** — agent-mode reminders fully suppressed (all `<system-reminder>` / `You are (in|NO LONGER in)` variants across text/content/type), send-button busy glow intensified (`22px` + `10px` with `onAccent`, forced opaque even when disabled).
- **v0.6.11 (2026-08-26)** — broadened plan-reminder filter to cover exit variant + tag + `content[]` shapes, bumped to avoid file collision.
- **v0.6.10 (2026-08-26)** — follow-up: system-reminder now suppressed for both enter/exit + `<system-reminder>` tag and `content[]` shapes (whole-UI tint remains sole indicator), agent/model switch noise fully removed, reasoning variant `default` → `Default` capitalization, stats footer `↑799 ↓96 ✻190 $0.0011` tabular alignment (`space-between` + `tabular-nums`), and question answers now always `steer` (never queue) so multi-question batches don’t stall.
- **v0.6.9 (2026-08-26)** — batch fixes: plan-mode dedup (suppress duplicate system reminder, whole-UI tint now works in both themes), question answers steer (not queue) and collapse optimistically, busy glow moved from top bar/caret to send button, model-switched chat noise removed, lazy first-session on first send, ECONNRESET retry suppressed + verbose hint stripped, send-arrow contrast via `--oc2-onAccent` (all themes), and “Start opencode2” CTA when no instance running (Windows hide + Linux cross-platform, opencode2-only).
- **v0.6.8 (2026-08-26)** — in-chat status: queued follow-ups render as ghost user bubbles at the feed tail (inbox.list; hover ✕ unqueues, click opens Inbox); dock retry card (attempt+action link) and compacting pill moved in-feed as transient sys-rows. Server verified live: queue lands in session inbox, delivers at end-of-run.
- **v0.6.7 (2026-08-26)** — steer/queue fix (composer submit hard-blocked on busy; busy sends now default steer / queue when toggled; server verified live). Previous:  — P0 fix ("new sessions unusable": fresh sessions had no model binding → server default blocked by provider data-policy → every prompt failed silently) + full seven-module batch: catalog-validated defaults (opencode2.models.default now **opencode/big-pickle**, free Zen) w/ free-Zen fallback (`pickNewSessionModel`), smart-retry model repair, execution-failure notice banner, prompt-aware smoke canary, native pre-apply diff review (`permission.asked.metadata.files[]` = FileDiff.Info, `src/diffPatch.ts` applier + `opencode-diff:` provider), CodeMode execute-tool visualizer + per-server codemode toggle (McpDrawer via config.get + runtime mcp.add), subagent inspector (child sessions via parentID — no subagent.* events exist), bespoke plan checklist (lib/plans.ts), reconnect banner w/ restart + last-session restore, own-mode health-probe fall-through. Client re-pinned to **beta-18230** matching the live server (interrupt returns payload; session.command requires text; event union: -integration.connection.updated, +credential.*, project.updated, persistent-pty.*).
- **Question stall fix (unreleased).** Root cause: `hasOpenQuestion` accepted `running|streaming` but auto-interrupt required exactly `running` — a question parked while `streaming` never got interrupted, answers steered into the parked run and hung. Fix: shared `isParkedQuestion()` helper (both states); interrupt success actively resyncs (`refreshMessages` + `refreshActiveSessions` + optimistic busy clear) instead of waiting on lossy SSE; interrupt failures capped at 3 attempts with a ■ Stop notice (was an infinite un-mark/retry storm); parked questions are interrupted once each and render as plain text (see below); Questions render as plain lettered text (`QuestionAsText`); the interactive `QuestionCard`/`OtherRow` forms and the whole answer overlay are removed — every stuck state lived in machinery promising a round-trip the backend can't complete. Replaced with a send-time `sessions.active` truth check in `sendMessage` (steer when the server says running, plain when idle; explicit chip wins; local flag only when unreachable) — fixes form answers and composer sends alike; `forcePlain` plumbing + helper removed. Second fix stands (live-verified on the real stuck session): aborted questions (`status:error`, run over) left `busySessions` true forever when the interrupt failed or no terminal SSE arrived — interrupt-failure now reconciles via `sessions.active`, plus a once-per-tool watchdog clearing stale busy for dead-unanswered questions (`isParked/isTerminalQuestionPart` in `lib/questions.ts`, unit-tested). Gate: typecheck · 160 tests · build · audit CLEAN.
- **Bug-fix sweep (unreleased).** P0: `sessionList` bare-array crash (`asRows`), `plan.save` workspace confinement, `vcs.diff`/`worktree.*` canonicalization, explicit `cliPath` direct resolve, `verify-hidden-start.mjs` ESM `fs` import, smoke canary canonicalized cwd. P1: question routes use live-connection target (`controller.getServiceTarget`, Flatpak/explicit-URL aware), `authCli` provider-name whitelist, `url.open` http(s)-only, `image.save` 11MB cap, `withTimeout` loser handled, SSE probe event delivered + iterator closed, explicit-loopback bracket/`::ffff` handling, question-probe 10s timeout + generation guard, autoCompact string-model guard + inflight guard, Composer slash-regex escape + 10MB image cap + preview truncation + chunked base64 + URL revoke, PlansDrawer ref-mirror + serialized saves, `providerRank` exact Zen ids, `isFreeCatalogModel` accepts `"0"`, permission actions trim/hyphen-normalized + `*_directory` widened, SubagentsDrawer busy only on open assistant turn, QuestionCard double-submit guard. Config: esbuild `node22` + watch CSS re-copy, test glob `test/**/*.test.ts`, `locations.ts` APPDATA-unset guard, `parseHostHeader` empty-port strip, `isLoopback` expanded `::1` form. Corrections to audit: `parseModelKey` was NOT buggy (id keeps post-first-slash content); `127.0.0.2` is intentionally remote (test-pinned) — only `127.0.0.1` is loopback. Gate: typecheck · 156 tests · build · audit CLEAN.
- **v0.3.37 (2026-08-25)** — full V2 GUI parity shipped. Docs consolidated: `plan.md`/`todo.md`/`AUDIT_AND_PLAN.md` deleted after completion (git history retains them). Living docs = this file + `agents.md` + `README.md`.
- **Support policy:** end users run their own CLI at whatever version they have — version skew is inherent. CI carries a non-blocking **beta-drift canary** (floating `@beta` install + gates) to catch upstream breakage early; the adapter's defensive normalization is the compatibility layer.
- **Version note:** v0.4.0 (2026-08-25) — docs consolidation + client re-pin.

## Current architecture facts

- **V2 only.** V1 desktop/TUI is UX-reference, never a source of API semantics. Ignore V2 `tui.*`/`installation.*` events. Slash catalog comes from V2 `command.list()`/`skill.list()`; never hardcode V1 TUI names.
- **GUI-first:** bespoke sidebar, not a terminal clone. Slash/skills/`@` = popovers; forms = `FormCard`; provider limits = action cards with links (`url.open` rpc).
- All server I/O in the extension host; webview is RPC-only over postMessage (`protocol.ts`). Every client call wrapped in `apiAdapter.ts`.
- Auto-start spawns `<cli> serve --service` ourselves (`detached` + `windowsHide` + `stdio:"ignore"`, unref'd) then polls `Service.discover()` ≤15s — NEVER `Service.ensure()` (its hard-coded spawn opens a console window on Windows).
- Event routing: explicit table in `webview-src/lib/events.ts` (drift-guarded against the installed SDK's V2Event union; `session.usage.recorded` existed in beta-18050 types but never its union, and is gone entirely in beta-18230 — keep unrouted; `.updated` drives session refetches; catalog drift events tick pickers). Delta streaming: `webview-src/lib/deltas.ts` (text/reasoning/tool deltas keyed by `assistantMessageID` + `ordinal`; tool progress appends into the matching tool card). REST re-sync remains the safety net; `refreshMessages` overlays longer local text/reasoning AND streamed tool output onto lagging snapshots.
- Feed autoscroll: bottom-first pin check (≤1px from max ⇒ pinned), directional unpin (>24px up, or 2% of feed), ResizeObserver on the content wrapper glued via rAF-coalesced floored scrollToBottom, `overflow-anchor:none`. New prompts force-jump and re-pin.
- Retries are SERVER-side (`session.retry.scheduled`, `SessionStatus: retry`, message.retry). UI: retry affordance lives INLINE on the failed assistant message (`Feed` + `assistantFailed()` in `lib/failure.ts` — finish:"error" / error obj / persisted retry marker), shown only while it's the newest message so it scrolls away naturally. `lib/rpc.ts` can't be imported in node tests (acquireVsCodeApi at module load) — keep testable helpers out of it. No client backoff.
- Permissions port upstream session auto-accept (`lib/permissions.ts`): auto-allow replies `"once"` per-session, questions stay interactive, `RespondedTracker` dedupes (1h TTL/1000 cap, cleared on reply failure), drain pending on enable. `"always"` persists rules but is manual-only. Server allow/deny config rules always win. **Rejecting one pending request rejects ALL pending in the same session** (OpenCode cancels them server-side) — `App` batch-replies + drains the UI together via `sameSessionPending()` (added v0.6.23+). `RespondedTracker.had/mark` take an injectable clock for tests. **Auto-acknowledged permissions (autoAllow or session-auto-accept) render as NON-BLOCKING plain text (`.perm-text` strip in the dock), NOT the interactive Allow/Deny `PermissionRow`** — only genuinely interactive (askFirst, not auto-accepted) perms show the card, so Deny stays possible for those (v0.6.25). On an authoritative re-sync (`refreshPendingPermissions(true)` on connect/resync), `lostReplyIds` clears the `RespondedTracker` mark for any request the server STILL lists pending (its reply was lost to an SSE drop/in-flight race) so the auto-reply effect re-sends it — otherwise the card stays and the shell command wedges forever.
- Shell/terminal output capped ~8 lines (internally scrollable; `fullShellOutput` overrides). Diff button opens VS Code side-by-side (HEAD↔worktree); inline preview capped too.
- share/unshare: no V2 API endpoints — permanently N/A.

## Gotchas

- Client pinned exact (`@opencode-ai/client@beta-18230` since v0.6.6 — matches live server); route all calls through `apiAdapter.ts`.
- Several list endpoints return BARE arrays (`form.list`, `session.export/import`, `inbox.list`, `permission.saved.list`, `worktree.list`) while others wrap `{data}` - adapter normalizes both shapes. **`agent.list`/`model.list` too** (v0.5.5): they 401-crashed pickers on bare-array servers - always route list calls through `asRows()`.
- Context % must come ONLY from assistant-step tokens via `liveContextStepTokens()` (`lib/format.ts`) - session-level `session.tokens` are CUMULATIVE lifetime usage (verified: 15M input/564M cache-read on one session) and survive compaction; compaction leaves a token-less `synthetic` marker, so steps at/before it are ignored and the meter honestly hides until a post-compact step lands. v0.5.5 briefly preferred session tokens - pegged the bar at 100%.
- **New-session model binding (P0 lesson):** never let fresh sessions run without an explicit model — a server default can be blocked by provider data-policy (OpenRouter privacy settings fail with `provider.invalid-request`). Defaults resolve via `pickNewSessionModel` (catalog-validated; free-Zen fallback). Edit permissions carry proposed diffs in `metadata.files[]`; CodeMode = per-server flag + one `execute` tool; subagents = child sessions (`parentID`), NOT events.
- **Drive-letter case (P0 lesson #2, win32):** a session location with a LOWERCASE drive letter (`e:\…`) crashes the server's instruction initializer ("Maximum call stack size exceeded" → `Instructions.InitializationBlocked`) — every prompt on that session silently persists nothing. VS Code can report workspace folders lowercase, so ALL directories sent to the server go through `canonicalizeDirectory()` (`src/directory.ts`; anchored on the COLON after the drive letter — `path.normalize("e:/x")` → `e:\x`, colon at index 1). Move-to-same-path-different-case is a no-op: poisoned sessions are unrecoverable, only prevention works.
- **Question tool needs server routes that NO published build ships:** `/api/question/request`, `/api/session/:id/question`, `/api/session/:id/question/:requestID/reply|reject` exist only in the unpublished OpenCode `dev` branch (Context7 confirms the protocol group). Verified live: `beta-18314` AND `dev-18341` both 404 all of them (0 mentions in `openapi.json`). So the agent `question` tool CANNOT complete through this GUI on any stable server — `question.reply` 404s (`QuestionHTTPUnavailable`), and a steered fallback message can't satisfy the agent's parked reply channel (causes the silent "sits and waits" hang). Client degrades gracefully (v0.6.22): card → `status:"unsupported"` + notice. Don't chase a server-side fix for distribution — end users won't have dev builds. **Code-side capability detection (v0.6.23):** the controller probes `/openapi.json` on connect (`detectQuestionSupport` in `controller.ts`) and sets `questionsSupported` in `ResolvedConfig.ui`; the webview renders `QuestionAsText` (plain markdown, no card/reply attempt) ALWAYS — the interactive `QuestionCard`/`OtherRow` were removed in v0.6.24 because the blocking "question window" got stuck on `running`/`streaming` and broke the conversation flow. Options render lettered a)/b)/c) inline; the user answers in chat. `detectQuestionSupport()` / `ui.questionsSupported` still exist in the controller/config but are now unused by the webview (no published server ships the routes anyway).
- **The client unwraps `{data}` itself** for single-resource endpoints (create/get verified live) - raw-HTTP wraps never reach the adapter; do NOT add unwrap layers. List endpoints still need `asRows` (bare arrays on some servers). New sessions: agent always "build"; model = last-used (`ui.lastModel` RPCs persisted in workspaceState) -> setting -> serverDefault. Webview has NO toast system - failures must surface via the App `notice` banner (silent catches were the new-session bug).
- Local services register with basic auth: discovery supplies creds via `Service.headers(endpoint)`, and explicit loopback `server.baseUrl` URLs now reuse a matching registration's auth (v0.5.5). Remote explicit URLs stay credential-free by design. Default serve port is 4096.
- Tool file parts are `{type:"file", uri, mime, name?}`; delta events key messages by `assistantMessageID` (+`ordinal`).
- V2 CLI binary is `opencode2` locally; npm `opencode-ai@beta` names its bin `opencode` - resolve dynamically, never hardcode. POSIX fallback locations (beyond PATH): `~/.local/bin`, `~/.npm-global/bin`, `~/.opencode/bin`, `~/bin`, `/usr/local/bin`, `/usr/bin` (`src/locations.ts`, platform-injectable + unit-tested) - GUI-launched editors don't inherit shell-profile PATH exports.
- **Flatpak (Linux):** VS Code as Flatpak sandboxes the extension host — host binaries/PATH/fs are invisible. We detect via `FLATPAK_ID` / `/.flatpak-info` / mountinfo (`src/flatpak.ts`, vscode-free for unit tests) and route every CLI action through `flatpak-spawn --host` (`spawnArgvHost` in `src/cli.ts`). CLI must be installed on the HOST. `whereAll` is cross-platform now (`where.exe` on win32, `sh -lc command -v -a` on POSIX, host-escaped on Flatpak) — no more Windows-only `where.exe` everywhere.
- **Flatpak XDG_STATE_HOME redirect:** the sandbox overrides it to `$HOME/.var/app/$ID/state` (≥1.13) while a HOST-spawned service registers at real `~/.local/state` — so `Service.discover()`'s default path NEVER sees the registration. Never call bare `Service.discover()`; use `controller.discoverService()` which probes `registrationFiles()` (`src/flatpak.ts`): `HOST_XDG_STATE_HOME` → canonical `~/.local/state` → env-based. Verified via lib source + flatpak-run(1).
- Permission reply is `client.permission.reply({sessionID, requestID, reply})` — top level, not under session. Fork needs a boundary (`{type:'through'}`).
- Client object does NOT expose baseUrl — controller tracks it. Client pkg is ESM-only with exports map → tsconfig needs `moduleResolution: "bundler"`.
- Webview CSP: nonce for scripts; `${webview.cspSource} 'unsafe-inline'` for styles.
- MCP add/remove over HTTP is runtime-scoped (config file untouched); credentials via `credential.update({credentialID,label})` + integration connect flows.
- Type predicates must target a subtype or narrowing silently no-ops.
- `.mjs` files cannot contain TS syntax; PowerShell mangles unicode in md files — use node scripts/edit tooling.
- vsce refuses `"private": true` in package.json.
- **README badges are intentionally STATIC** (flat style, hard-coded hex): the dynamic `img.shields.io/github/license/...` lookup 404s ("not identifiable by github") because the repo is private/unreadable by shields, so the license badge is a hardcoded `MIT`. Sponsor badge links to `github.com/sponsors/Mrakulis` and uses the app's build-mode accent purple (`#7C5CFF`, muted `#4A3796`) — not the bright `#7C5CFF` default. No Marketplace version/installs badges until the extension is actually published there.

## Environment (Windows box)

- win32 · pwsh · Node 22.20 / npm 11.14 · git 2.51
- graphify CLI 0.9.48; post-commit hook rebuilds graph on code commits (docs-only commits skip)
- Graphify LLM key in `.env` as `GEMINI_API_KEY=...` (**gitignored — never commit/print**)
- V2 OpenAPI spec cached at `%TEMP%\opencode-v2-openapi.json` (re-download from https://opencode.ai/v2/openapi.json if missing)

## Session protocol

1. Read `agents.md` → this file before working.
2. Gate every change: `npm run typecheck` → `npm test` → `npm run build` (+ live smoke where feasible).
3. Update this file when a durable fact/decision appears; keep it consolidated, not append-only.
4. Conventional commit → push → verify `graphify-out/` refreshed on code commits.
